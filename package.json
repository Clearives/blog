{
  "name": "clearives.cc-blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vuepress dev ./",
    "build": "./node_modules/.bin/vuepress build .",
    "cp": "cp -r ./dist/ ../clearives.github.io",
    "deploy": "npm run build && npm run cp"
  },
  "keywords": [],
  "author": "clearives",
  "license": "ISC",
  "dependencies": {
    "vuepress": "^1.0.2",
    "vuepress-plugin-comment": "^0.5.4"
  }
}
